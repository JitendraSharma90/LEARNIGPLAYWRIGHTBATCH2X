//single quote string literal
let singleQuoteStr = 'Hello, World!';
//double quote string literal
let doubleQuoteStr = "Hello, World!";

let a = 'm';
let b1 = "m";
console.log(typeof a); // string
console.log(typeof b1); // string
    //javascrript prefer to use single quote string literal but we can use double quote string literal as well. It is a matter of choice and consistency in code style.

    